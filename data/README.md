# Data directories

- `raw/`: untouched API responses
- `interim/`: normalized and transformed working data
- `curated/`: manually validated corpus metadata

Large or reproducible downloaded files should not normally be committed to Git.
